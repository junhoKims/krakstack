import type { Config } from 'prettier';

/**
 * Prettier `base` 설정
 */
export declare const baseConfig: Config;
/**
 * Prettier `with-tailwindcss` 설정
 */
export declare const withTailwindcssConfig: Config;